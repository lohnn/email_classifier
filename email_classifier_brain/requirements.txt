# =============================================================================
# Email Classification System — Python Dependencies
# =============================================================================
# Install with:  pip install -r requirements.txt
#
# SYSTEM DEPENDENCY (not a pip package):
#   git-lfs — required for versioning the trained model via Git.
#   Install on Debian/Ubuntu/Raspberry Pi OS:
#       sudo apt-get install git-lfs && git lfs install
#   Install on macOS:
#       brew install git-lfs && git lfs install
# =============================================================================

setfit>=1.0.0
datasets>=2.14.0
torch>=2.0.0
transformers>=4.30.0,<5.0.0  # setfit 1.1.x requires <5.0
sentence-transformers>=2.2.0
python-dotenv>=1.0.0

# API & Scheduler
fastapi>=0.100.0
uvicorn>=0.23.0
apscheduler>=3.10.0

# Testing
pytest
httpx
